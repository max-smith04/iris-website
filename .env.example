# Neon Postgres — use the POOLED connection string (its host contains "-pooler").
# Neon Console -> your project -> Connect -> Connection string -> tick "Pooled connection".
DATABASE_URL="postgresql://USER:PASSWORD@ep-example-123456-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require"
